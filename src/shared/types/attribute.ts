export interface AttributeDto {
  id: string;
  name: string;
  dataType: string;
}

export interface CreateAttributeDto {
  name: string;
  dataType: string;
}

export interface UpdateAttributeDto {
  name: string;
  dataType: string;
}
